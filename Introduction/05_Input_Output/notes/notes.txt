the standard library for c has 2 built in functions to process character data.

c = getchar();
getchar() reads the next input character from a text stream and returns that value.

putchar(c);
putchar(character) prints the parameterized character.
